name: Run unit tests

on: push

jobs:
  test:
    name: 🧪 Run unit tests
    runs-on: ubuntu-latest
    steps:
      - name: 📁 Check out code
        uses: actions/checkout@v4

      - name: Install dependencies
        run: "npm install"
  
      - name: 🧪 Run tests
        run: "npm test"
  

